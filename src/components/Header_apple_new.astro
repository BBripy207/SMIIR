---
// Header estilo Apple con tarjeta grande para SIMIR
---

<style>
/* Header estilo Apple con tarjeta grande */
.apple-header {
    backgrou    <div class="header-content">
        <img src="/assets/images/simir.png" alt="SIMIR" class="logo">
        
        <a href="/productos" class="products-card">
            <img src="/assets/images/1.png" alt="DINERBOT T10" class="product-icon-large">
            <div class="product-text">
                <h2 class="product-title">Productos</h2>
            </div>
        </a>
    </div>.92);
    backdrop-filter: blur(20px);
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: 12px 0;
    min-height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.header-content {
    max-width: 1200px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    padding: 0 20px;
    gap: 40px;
}

.logo {
    height: 36px;
    width: auto;
    flex-shrink: 0;
}

/* Tarjeta grande de Productos estilo iPhone */
.products-card {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.15) 0%, rgba(118, 75, 162, 0.15) 100%);
    border: 1px solid rgba(102, 126, 234, 0.3);
    border-radius: 16px;
    padding: 6px 4px 4px 4px;
    text-decoration: none;
    color: #f5f5f7;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    width: 90px;
    height: 110px;
    backdrop-filter: blur(10px);
}

.products-card:hover {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.25) 0%, rgba(118, 75, 162, 0.25) 100%);
    border-color: rgba(102, 126, 234, 0.5);
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(102, 126, 234, 0.2);
    color: #ffffff;
}

.product-icon-large {
    width: 68px;
    height: 68px;
    border-radius: 12px;
    object-fit: cover;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
}

.product-text {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    margin-top: 0;
    flex: 1;
    justify-content: center;
}

.product-title {
    font-size: 12px;
    font-weight: 400;
    margin: 0;
    line-height: 1;
    text-align: center;
}

.contact-section {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-shrink: 0;
}

.contact-link {
    color: #f5f5f7;
    text-decoration: none;
    font-size: 18px;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.contact-link:hover {
    color: #ffffff;
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.2);
    transform: translateY(-1px);
}

/* Responsive */
@media (max-width: 768px) {
    .apple-header {
        min-height: 140px;
        padding: 8px 0;
        overflow-x: hidden;
        width: 100vw;
        box-sizing: border-box;
    }
    
    .header-content {
        flex-direction: row;
        flex-wrap: nowrap;
        gap: 8px;
        padding: 8px 10px;
        justify-content: space-between;
        align-items: center;
        max-width: 100%;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
        scrollbar-width: none; /* Firefox */
        -ms-overflow-style: none; /* IE/Edge */
    }
    
    .header-content::-webkit-scrollbar {
        display: none; /* Chrome/Safari */
    }
    
    .logo {
        height: 35px;
        flex-shrink: 0;
    }
    
    .products-card {
        width: 70px;
        height: 90px;
        padding: 3px 2px 2px 2px;
        gap: 1px;
        flex-shrink: 0;
    }
    
    .product-icon-large {
        width: 50px;
        height: 50px;
    }
    
    .product-title {
        font-size: 10px;
    }
    
    .contact-section {
        display: flex;
        flex-shrink: 0;
        gap: 8px;
    }
    
    .contact-link {
        width: 36px;
        height: 36px;
        font-size: 14px;
    }
}
</style>

<header class="apple-header">
    <div class="header-content">
        <img src="/assets/images/simir.png" alt="SIMIR" class="logo">
        
        <a href="/productos" class="products-card">
            <img src="/assets/images/1.png" alt="DINERBOT T10" class="product-icon-large">
            <div class="product-text">
                <h2 class="product-title">Productos</h2>
            </div>
        </a>
        
        <div class="contact-section">
            <a href="tel:+526145518651" class="contact-link" title="Llamar">
                📞
            </a>
            <a href="https://wa.me/message/PGBVC2B2SREDG1" class="contact-link" target="_blank" title="WhatsApp">
                <i class="fab fa-whatsapp"></i>
            </a>
        </div>
    </div>
</header>

<!-- Espaciado para el contenido debajo del header fijo -->
<style>
main, .presentation-container {
    margin-top: 100px;
}

@media (max-width: 768px) {
    main, .presentation-container {
        margin-top: 160px;
    }
}
</style>